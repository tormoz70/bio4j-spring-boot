create or replace procedure test_stored_long(p_param1 in varchar2, p_param2 out number)
  is
  begin
    null;
    --insert into test_tbl values('test', 1, null); p_param2 := length(p_param1);
    --dbms_lock.sleep(120);
  end;
